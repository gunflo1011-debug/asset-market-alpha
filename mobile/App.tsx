import React, { useEffect, useMemo, useState } from 'react';
import type { Session } from '@supabase/supabase-js';
import { ActivityIndicator, Linking, SafeAreaView, ScrollView, StyleSheet, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { beginPasswordRecoveryFromUrl, getSession, onAuthStateChange, requestAccountEmailChange, requestPasswordReset, signIn, signOut, signUp, updateAccountPassword, updateRecoveredPassword } from './src/data/auth';
import { addPrivateDevice, CatalogVariant, loadCatalog, loadPrivateInventory, PrivateInventoryItem } from './src/data/inventory';
import { buildSaleStartSurface } from './src/lib/saleStartSurface';
import { hasSupabaseConfig } from './src/lib/supabase';

type AuthMode = 'signin' | 'signup' | 'forgot' | 'recovery';
function variantTitle(variant: CatalogVariant): string { const product = variant.products; const base = product ? `${product.brand} ${product.family}` : 'Device'; return `${base}${variant.storage_gb ? ` · ${variant.storage_gb} GB` : ''}`; }
function itemTitle(item: PrivateInventoryItem): string { const variant = item.product_variants; const product = variant?.products; if (!variant || !product) return 'Private device'; return `${product.brand} ${product.family}${variant.storage_gb ? ` · ${variant.storage_gb} GB` : ''}`; }
function friendlyAuthError(error: unknown): string { const raw = error instanceof Error ? error.message.toLowerCase() : ''; if (raw.includes('invalid login credentials')) return 'Email or password is incorrect.'; if (raw.includes('email not confirmed')) return 'Please confirm your email before signing in.'; if (raw.includes('rate limit')) return 'Too many attempts. Please wait a moment and try again.'; if (raw.includes('expired')) return 'This link has expired. Request a new password-reset email.'; if (raw.includes('invalid email')) return 'Enter a valid email address.'; if (raw.includes('same password')) return 'Choose a password you have not used for this account.'; return error instanceof Error ? error.message : 'Authentication failed. Please try again.'; }

export default function App() {
  const [session, setSession] = useState<Session | null>(null); const [authReady, setAuthReady] = useState(false); const [authMode, setAuthMode] = useState<AuthMode>('signin'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [items, setItems] = useState<PrivateInventoryItem[]>([]); const [catalog, setCatalog] = useState<CatalogVariant[]>([]); const [selectedVariantId, setSelectedVariantId] = useState<string | null>(null); const [busy, setBusy] = useState(false); const [message, setMessage] = useState<string | null>(null); const [showAccount, setShowAccount] = useState(false); const [accountEmail, setAccountEmail] = useState(''); const [accountPassword, setAccountPassword] = useState(''); const [accountPasswordConfirm, setAccountPasswordConfirm] = useState(''); const [saleIntentItemId, setSaleIntentItemId] = useState<string | null>(null);
  const selectedVariant = useMemo(() => catalog.find((variant) => variant.id === selectedVariantId) ?? null, [catalog, selectedVariantId]);
  useEffect(() => { if (!hasSupabaseConfig) { setAuthReady(true); return; } let active = true; getSession().then((next) => { if (active) setSession(next); }).catch((error: Error) => { if (active) setMessage(error.message); }).finally(() => { if (active) setAuthReady(true); }); const unsubscribe = onAuthStateChange((next) => setSession(next)); return () => { active = false; unsubscribe(); }; }, []);
  useEffect(() => { if (!hasSupabaseConfig) return; let active = true; const handleRecoveryUrl = async (url: string | null) => { if (!url || !url.startsWith('thingsalpha://auth/reset-password')) return; try { setBusy(true); setMessage(null); await beginPasswordRecoveryFromUrl(url); if (!active) return; setPassword(''); setConfirmPassword(''); setAuthMode('recovery'); } catch (error) { if (!active) return; setAuthMode('forgot'); setMessage(friendlyAuthError(error)); } finally { if (active) setBusy(false); } }; void Linking.getInitialURL().then(handleRecoveryUrl); const subscription = Linking.addEventListener('url', ({ url }) => void handleRecoveryUrl(url)); return () => { active = false; subscription.remove(); }; }, []);
  useEffect(() => { if (!session || authMode === 'recovery') { setItems([]); setCatalog([]); setShowAccount(false); return; } setAccountEmail(session.user.email ?? ''); void refreshData(); }, [session, authMode]);
  async function refreshData() { try { setBusy(true); setMessage(null); const [nextItems, nextCatalog] = await Promise.all([loadPrivateInventory(), loadCatalog()]); setItems(nextItems); setCatalog(nextCatalog); setSelectedVariantId((current) => current ?? nextCatalog[0]?.id ?? null); } catch (error) { setMessage(error instanceof Error ? error.message : 'Could not load private inventory.'); } finally { setBusy(false); } }
  async function authenticate(mode: 'signin' | 'signup') { if (!email.trim() || password.length < 6) { setMessage('Enter an email and a password with at least 6 characters.'); return; } try { setBusy(true); setMessage(null); if (mode === 'signin') await signIn(email.trim(), password); else setMessage(await signUp(email.trim(), password)); } catch (error) { setMessage(friendlyAuthError(error)); } finally { setBusy(false); } }
  async function sendPasswordReset() { const normalizedEmail = email.trim(); if (!normalizedEmail || !normalizedEmail.includes('@')) { setMessage('Enter the email address you use for Things.'); return; } try { setBusy(true); setMessage(null); await requestPasswordReset(normalizedEmail); setMessage('If an account exists for this email, a password-reset link has been sent.'); } catch (error) { setMessage(friendlyAuthError(error)); } finally { setBusy(false); } }
  async function finishPasswordRecovery() { if (password.length < 8) { setMessage('Use at least 8 characters for your new password.'); return; } if (password !== confirmPassword) { setMessage('The passwords do not match.'); return; } try { setBusy(true); setMessage(null); await updateRecoveredPassword(password); setPassword(''); setConfirmPassword(''); setAuthMode('signin'); setMessage('Password updated. Sign in with your new password.'); } catch (error) { setMessage(friendlyAuthError(error)); } finally { setBusy(false); } }
  async function saveAccountEmail() { const normalized = accountEmail.trim(); if (!normalized || !normalized.includes('@')) { setMessage('Enter a valid email address.'); return; } if (normalized.toLowerCase() === (session?.user.email ?? '').toLowerCase()) { setMessage('This is already your current login email.'); return; } try { setBusy(true); setMessage(null); await requestAccountEmailChange(normalized); setMessage('Email change requested. Follow the verification email before using the new address to sign in.'); } catch (error) { setMessage(friendlyAuthError(error)); } finally { setBusy(false); } }
  async function saveAccountPassword() { if (accountPassword.length < 8) { setMessage('Use at least 8 characters for your new password.'); return; } if (accountPassword !== accountPasswordConfirm) { setMessage('The new passwords do not match.'); return; } try { setBusy(true); setMessage(null); await updateAccountPassword(accountPassword); setAccountPassword(''); setAccountPasswordConfirm(''); setMessage('Password updated successfully.'); } catch (error) { setMessage(friendlyAuthError(error)); } finally { setBusy(false); } }
  function switchAuthMode(mode: AuthMode) { setAuthMode(mode); setMessage(null); if (mode === 'forgot') setPassword(''); setConfirmPassword(''); }
  async function createPrivateDevice() { if (!selectedVariantId) return; try { setBusy(true); setMessage(null); await addPrivateDevice({ variantId: selectedVariantId }); await refreshData(); setMessage('Device saved privately. It is not publicly listed.'); } catch (error) { setMessage(error instanceof Error ? error.message : 'Could not save device.'); } finally { setBusy(false); } }
  if (!authReady) return <SafeAreaView style={styles.centered}><ActivityIndicator /><Text style={styles.muted}>Restoring secure session…</Text></SafeAreaView>;
  if (!hasSupabaseConfig) return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.eyebrow}>PRIVATE INVENTORY ALPHA</Text><Text style={styles.title}>Backend connection required.</Text><Text style={styles.subtitle}>This build intentionally does not fall back to fake inventory. Configure EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY to test the real private-data flow.</Text></View></SafeAreaView>;
  if (authMode === 'recovery') return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.eyebrow}>THINGS · ACCOUNT RECOVERY</Text><Text style={styles.title}>Choose a new password.</Text><Text style={styles.subtitle}>Your reset link was verified. Set a new password to secure your account.</Text><View style={styles.card}><TextInput value={password} onChangeText={setPassword} placeholder="New password" secureTextEntry style={styles.input} /><TextInput value={confirmPassword} onChangeText={setConfirmPassword} placeholder="Confirm new password" secureTextEntry style={styles.input} /><TouchableOpacity style={[styles.primaryButton, (busy || password.length < 8 || password !== confirmPassword) ? styles.disabled : null]} disabled={busy || password.length < 8 || password !== confirmPassword} onPress={() => void finishPasswordRecovery()}><Text style={styles.primaryButtonText}>{busy ? 'Updating…' : 'Update password'}</Text></TouchableOpacity>{message ? <Text style={styles.helper}>{message}</Text> : null}</View></View></SafeAreaView>;
  if (!session) return <SafeAreaView style={styles.safe}><View style={styles.container}><Text style={styles.eyebrow}>THINGS · PRIVATE BY DEFAULT</Text><Text style={styles.title}>{authMode === 'forgot' ? 'Reset your password.' : authMode === 'signup' ? 'Create your Things account.' : 'Welcome back.'}</Text><Text style={styles.subtitle}>{authMode === 'forgot' ? 'Enter your email and we will send a secure reset link. We never reveal whether an account exists.' : 'Your inventory starts private and stays under your control.'}</Text><View style={styles.card}><TextInput value={email} onChangeText={setEmail} placeholder="Email" autoCapitalize="none" keyboardType="email-address" style={styles.input} />{authMode !== 'forgot' ? <TextInput value={password} onChangeText={setPassword} placeholder="Password" secureTextEntry style={styles.input} /> : null}{authMode === 'signin' ? <><TouchableOpacity style={styles.primaryButton} disabled={busy} onPress={() => void authenticate('signin')}><Text style={styles.primaryButtonText}>{busy ? 'Signing in…' : 'Sign in'}</Text></TouchableOpacity><TouchableOpacity disabled={busy} onPress={() => switchAuthMode('forgot')}><Text style={styles.linkCentered}>Forgot password?</Text></TouchableOpacity><TouchableOpacity style={styles.secondaryButton} onPress={() => switchAuthMode('signup')}><Text style={styles.secondaryButtonText}>Create account</Text></TouchableOpacity></> : null}{authMode === 'signup' ? <><TouchableOpacity style={styles.primaryButton} disabled={busy} onPress={() => void authenticate('signup')}><Text style={styles.primaryButtonText}>{busy ? 'Creating account…' : 'Create account'}</Text></TouchableOpacity><TouchableOpacity style={styles.secondaryButton} onPress={() => switchAuthMode('signin')}><Text style={styles.secondaryButtonText}>Back to sign in</Text></TouchableOpacity></> : null}{authMode === 'forgot' ? <><TouchableOpacity style={styles.primaryButton} disabled={busy} onPress={() => void sendPasswordReset()}><Text style={styles.primaryButtonText}>{busy ? 'Sending…' : 'Send reset link'}</Text></TouchableOpacity><TouchableOpacity style={styles.secondaryButton} onPress={() => switchAuthMode('signin')}><Text style={styles.secondaryButtonText}>Back to sign in</Text></TouchableOpacity></> : null}{message ? <Text style={styles.helper}>{message}</Text> : null}</View></View></SafeAreaView>;
  if (showAccount) return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.container}><View style={styles.rowBetween}><View style={styles.flex}><Text style={styles.eyebrow}>THINGS · ACCOUNT</Text><Text style={styles.title}>Account & Security</Text></View><TouchableOpacity onPress={() => setShowAccount(false)}><Text style={styles.link}>Done</Text></TouchableOpacity></View><View style={styles.card}><Text style={styles.sectionTitle}>Sign-in email</Text><Text style={styles.helper}>Current: {session.user.email ?? 'No email available'}</Text><TextInput value={accountEmail} onChangeText={setAccountEmail} placeholder="New email address" style={styles.input} /><TouchableOpacity style={styles.secondaryButton} onPress={() => void saveAccountEmail()}><Text style={styles.secondaryButtonText}>Change email</Text></TouchableOpacity></View><View style={styles.card}><Text style={styles.sectionTitle}>Password</Text><TextInput value={accountPassword} onChangeText={setAccountPassword} placeholder="New password" secureTextEntry style={styles.input} /><TextInput value={accountPasswordConfirm} onChangeText={setAccountPasswordConfirm} placeholder="Confirm new password" secureTextEntry style={styles.input} /><TouchableOpacity style={styles.primaryButton} onPress={() => void saveAccountPassword()}><Text style={styles.primaryButtonText}>Update password</Text></TouchableOpacity></View>{message ? <Text style={styles.notice}>{message}</Text> : null}<TouchableOpacity style={styles.secondaryButton} onPress={() => void signOut()}><Text style={styles.secondaryButtonText}>Sign out</Text></TouchableOpacity></ScrollView></SafeAreaView>;
  return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.container}><View style={styles.rowBetween}><View style={styles.flex}><Text style={styles.eyebrow}>PRIVATE INVENTORY ALPHA</Text><Text style={styles.title}>My devices</Text></View><TouchableOpacity onPress={() => { setAccountEmail(session.user.email ?? ''); setMessage(null); setShowAccount(true); }}><Text style={styles.link}>Account</Text></TouchableOpacity></View><View style={styles.card}><Text style={styles.metric}>{items.length}</Text><Text style={styles.metricLabel}>private device{items.length === 1 ? '' : 's'}</Text><Text style={styles.helper}>Total known inventory value: unavailable until verified value evidence exists. Unknown values are never counted as €0.</Text></View><View style={styles.card}><Text style={styles.sectionTitle}>Add a device privately</Text>{catalog.slice(0, 8).map((variant) => { const selected = variant.id === selectedVariantId; return <TouchableOpacity key={variant.id} style={[styles.variantButton, selected ? styles.variantButtonSelected : null]} onPress={() => setSelectedVariantId(variant.id)}><Text style={styles.variantText}>{variantTitle(variant)}</Text></TouchableOpacity>; })}<TouchableOpacity style={[styles.primaryButton, !selectedVariant ? styles.disabled : null]} disabled={!selectedVariant || busy} onPress={() => void createPrivateDevice()}><Text style={styles.primaryButtonText}>{busy ? 'Saving…' : 'Add privately'}</Text></TouchableOpacity><Text style={styles.helper}>Creating an item never creates a public marketplace listing.</Text></View>{message ? <Text style={styles.notice}>{message}</Text> : null}<View style={styles.rowBetween}><Text style={styles.sectionTitle}>Inventory</Text><TouchableOpacity disabled={busy} onPress={() => void refreshData()}><Text style={styles.link}>Refresh</Text></TouchableOpacity></View>{items.length === 0 && !busy ? <View style={styles.card}><Text style={styles.helper}>No devices yet. Add the first one above.</Text></View> : null}{items.map((item) => { const snapshot = item.condition_snapshots[0]; const sale = buildSaleStartSurface(item.id, null); const open = saleIntentItemId === item.id; return <View key={item.id} style={styles.itemCard}><Text style={styles.itemTitle}>{itemTitle(item)}</Text><Text style={styles.muted}>Condition: {snapshot?.housing_state ?? 'not captured'}</Text>{snapshot?.battery_health != null ? <Text style={styles.muted}>Battery health: {snapshot.battery_health}%</Text> : null}<Text style={styles.valueLabel}>{sale.valueLabel}</Text><TouchableOpacity style={styles.secondaryButton} onPress={() => setSaleIntentItemId(open ? null : item.id)}><Text style={styles.secondaryButtonText}>{sale.actionLabel}</Text></TouchableOpacity>{open ? <View style={styles.saleDecision}><Text style={styles.helper}>{sale.privacyNotice}</Text><Text style={styles.helper}>Value evidence is currently unknown, so Things will not invent an asking price. This private decision step does not create a listing.</Text></View> : null}<View style={styles.badge}><Text style={styles.badgeText}>PRIVATE · eligible for demand matching</Text></View></View>; })}<View style={styles.card}><Text style={styles.sectionTitle}>Alpha rule</Text><Text style={styles.helper}>No public inventory browsing and no automatic sale. Selling always starts with an explicit private owner decision.</Text></View></ScrollView></SafeAreaView>;
}
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F4F5F7' }, centered: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 12, backgroundColor: '#F4F5F7' }, container: { padding: 20, gap: 14 }, eyebrow: { fontSize: 12, fontWeight: '700', letterSpacing: 1.2, color: '#616A76' }, title: { fontSize: 30, lineHeight: 35, fontWeight: '800', color: '#101828' }, subtitle: { fontSize: 16, lineHeight: 23, color: '#475467' }, card: { backgroundColor: '#FFFFFF', borderRadius: 18, padding: 18, gap: 10 }, metricLabel: { fontSize: 14, color: '#667085' }, metric: { fontSize: 34, fontWeight: '800', color: '#101828' }, muted: { fontSize: 13, color: '#667085' }, valueLabel: { fontSize: 14, fontWeight: '700', color: '#344054' }, sectionTitle: { fontSize: 18, fontWeight: '700', color: '#101828' }, input: { borderWidth: 1, borderColor: '#D0D5DD', borderRadius: 12, paddingHorizontal: 14, paddingVertical: 12, fontSize: 16, backgroundColor: '#FFFFFF' }, primaryButton: { borderRadius: 12, paddingVertical: 13, alignItems: 'center', backgroundColor: '#101828' }, primaryButtonText: { color: '#FFFFFF', fontWeight: '700', fontSize: 15 }, secondaryButton: { borderRadius: 12, paddingVertical: 13, alignItems: 'center', borderWidth: 1, borderColor: '#D0D5DD' }, secondaryButtonText: { color: '#344054', fontWeight: '700', fontSize: 15 }, helper: { fontSize: 13, lineHeight: 19, color: '#667085' }, notice: { fontSize: 13, lineHeight: 19, color: '#344054', backgroundColor: '#FFFFFF', padding: 12, borderRadius: 12 }, itemCard: { backgroundColor: '#FFFFFF', borderRadius: 16, padding: 16, gap: 8 }, saleDecision: { backgroundColor: '#F8FAFC', borderRadius: 12, padding: 12, gap: 6 }, rowBetween: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', gap: 12 }, flex: { flex: 1 }, itemTitle: { fontSize: 16, fontWeight: '700', color: '#101828' }, badge: { alignSelf: 'flex-start', borderRadius: 999, backgroundColor: '#EEF2F6', paddingHorizontal: 10, paddingVertical: 6 }, badgeText: { fontSize: 12, fontWeight: '600', color: '#344054' }, link: { fontSize: 14, fontWeight: '700', color: '#344054' }, linkCentered: { fontSize: 14, fontWeight: '700', color: '#344054', textAlign: 'center', paddingVertical: 4 }, variantButton: { borderWidth: 1, borderColor: '#D0D5DD', borderRadius: 10, padding: 11 }, variantButtonSelected: { borderWidth: 2, borderColor: '#101828' }, variantText: { fontSize: 14, color: '#344054' }, disabled: { opacity: 0.45 } });